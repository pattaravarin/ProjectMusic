<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- ชื่อเว็บ -->
    <title>Utae</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <link rel="stylesheet" href="/stylesheets/mainn.css">
</head>
<body>
    <!-- เป็นส่วน Navbar ทั้งหมด ซึ่งแน่นอนก็อปเขามา -->
<header class="p-3 bg-dark text-white">
  <div class="container">
    <div class="d-flex flex-wrap align-items-center justify-content-center justify-content-lg-start">
      <a href="/" class="d-flex align-items-center mb-2 mb-lg-0 text-white text-decoration-none">
        <svg class="bi me-2" width="40" height="32" role="img" aria-label="Bootstrap"><use xlink:href="#bootstrap"/></svg>
      </a>

      <ul class="nav col-12 col-lg-auto me-lg-auto mb-2 justify-content-center mb-md-0">
        <li><a href="/" class="nav-link px-2 text-secondary">Utae</a></li>
        <li><a href="/prints" class="nav-link px-2 text-white">รวมเพลง</a></li>
        <li><a href="/artist" class="nav-link px-2 text-white">ศิลปิน</a></li>
        <!-- <li><a href="#" class="nav-link px-2 text-white">โปรไฟล์</a></li> -->
      </ul>

      <form class="col-12 col-lg-auto mb-3 mb-lg-0 me-lg-3" action="/search" method="post">
        <input type="search" class="form-control form-control-dark" name="searchButton" placeholder="Search..." aria-label="Search">
      </form>

      <% if(!currentUser){ %>
        <a href="/login" type="button" class="btn btn-outline-light me-2">Login</a>
        <a href="/register" type="button" class="btn btn-warning">Sing-up</a>
      <% }else { %>
        <a href="/user/<%= currentUser._id %>" type="button" class="btn btn-outline-light me-2"><%= currentUser.username %></a>
        <a href="/logout" type="button" class="btn btn-warning">Log out</a>
        
      <% } %>
      <!-- <div class="text-end">
        <button type="button" class="btn btn-outline-light me-2">Login</button>
        <button type="button" class="btn btn-warning">Sign-up</button>
      </div> -->
    </div>
  </div>
</header>
</body>
<% if(error &&error.length>0){ %>
  <div class='alert alert-danger' role='alert'>
    <%= error %>
  </div>
<% } %>
<% if(success &&success.length>0){ %>
  <div class='alert alert-success' role='alert'>
    <%= success %>
  </div>
<% } %>

