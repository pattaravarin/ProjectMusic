<%- include("../partials/header.ejs") %>
<!-- <h1 class="textCenter">User Profile</h1>
<h1 class="textCenter"><%= user.firstName + ' ' + user.lastName %></h1>
<img src="<%= user.profileImage %>">
<p>
    <a href='mailto : <%= user.email %>'> <%= user.email %></a>
</p> -->

<link href="https://maxcdn.bootstrapcdn.com/font-awesome/4.3.0/css/font-awesome.min.css" rel="stylesheet">
<div class="container bootstrap snippets bootdey">
<div class="row">
  <div class="profile-nav col-md-3">
      <div class="panel">
          <div class="user-heading round">
              <a href="#">
                  <img src="<%= user.profileImage %>">
              </a>
              <h1><%= user.firstName + ' ' + user.lastName %></h1>
              <p><%= user.email %></p>
          </div>

<% if (currentUser || currentUser.isAdmin){ %>
          <ul class="nav nav-pills nav-stacked">
              <li><a href="/user/<%= currentUser._id %>/edit"> <i class="fa fa-edit"></i> Edit profile</a></li>
          </ul>
          <% if (currentUser.isAdmin){ %>
          <a href='/user/delete' class="btn btn-dark">Go back</a>
          <% } %>
<% } %>
      </div>
  </div>
  <div class="profile-info col-md-9">
      <div class="panel">
          <form>
              <textarea placeholder="Whats in your mind today?" rows="2" class="form-control input-lg p-text-area"></textarea>
          </form>
          <footer class="panel-footer">
              <button class="btn btn-warning pull-right">Post</button>
              <ul class="nav nav-pills">
                  <li>
                      <a href="#"><i class="fa fa-map-marker"></i></a>
                  </li>
                  <li>
                      <a href="#"><i class="fa fa-camera"></i></a>
                  </li>
                  <li>
                      <a href="#"><i class=" fa fa-film"></i></a>
                  </li>
                  <li>
                      <a href="#"><i class="fa fa-microphone"></i></a>
                  </li>
              </ul>
          </footer>
      </div>
      <div class="panel">
          <div class="bio-graph-heading">
              ยางม่ายได้นอนนนนนน
          </div>
          <div class="panel-body bio-graph-info">
              <div class="row">
                  <div class="bio-row">
                    <h1 class="textCenter"><%= user.firstName %></h1>
                  </div>
                  <div class="bio-row">
                    <h1 class="textCenter"><%= user.lastName %></h1>
                  </div>
                 
                  <div class="bio-row">
                    <p>
                        <a href='mailto : <%= user.email %>'> <%= user.email %></a>
                    </p>
                  </div>
              </div>
          </div>
      </div>
  </div>
</div>
</div>


<%- include("../partials/footer.ejs") %>